"""Flex header library. Used by the C++ lexer class."""

licenses(["notice"])  # BSD

cc_library(
    name = "flex_lexer",
    hdrs = ["src/FlexLexer.h"],
    includes = ["src"],
    include_prefix = "flex",
    visibility = ["//visibility:public"],
)
